#! /bin/zsh

# Omit "cd"
setopt AUTO_CD

setopt CORRECT

alias ll='ls --color=always --group-directories-first -Al'
alias ls='ls --color=always --group-directories-first'

export LANGUAGE="en_US.UTF-8"
export LC_ALL="en_US.UTF-8"
export LANG="en_US.UTF-8"
